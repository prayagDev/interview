182. Duplicate Emails
Easy
Topics
premium lock icon
Companies
SQL Schema
Pandas Schema
Table: Person

+-------------+---------+
| Column Name | Type    |
+-------------+---------+
| id          | int     |
| email       | varchar |
+-------------+---------+
id is the primary key (column with unique values) for this table.
Each row of this table contains an email. The emails will not contain uppercase letters.
 

Write a solution to report all the duplicate emails. Note that it's guaranteed that the email field is not NULL.

Return the result table in any order.

The result format is in the following example.

 

Example 1:

Input: 
Person table:
+----+---------+
| id | email   |
+----+---------+
| 1  | a@b.com |
| 2  | c@d.com |
| 3  | a@b.com |
+----+---------+
Output: 
+---------+
| Email   |
+---------+
| a@b.com |
+---------+
Explanation: a@b.com is repeated two times.




Answer
--------------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------------

This is a classic use case for GROUP BY + HAVING.

Solution
SELECT email AS Email
FROM Person
GROUP BY email
HAVING COUNT(*) > 1;
Explanation
GROUP BY email
Groups all rows having the same email together.
COUNT(*)
Counts how many rows are present in each email group.
HAVING COUNT(*) > 1
Filters only those groups whose count is greater than 1 (duplicates).
AS Email
Renames the output column to Email as required by the problem statement.


Why HAVING instead of WHERE?
WHERE filters rows before grouping.
HAVING filters groups after aggregation.

Since COUNT(*) is an aggregate function, you must use HAVING.

